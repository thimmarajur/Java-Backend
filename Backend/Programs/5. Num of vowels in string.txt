package com.cms.admin.controller;

import java.util.Arrays;
import java.util.stream.Collectors;
import java.util.stream.IntStream;

public class TestClass {

	public static void main(String[] args) {
		String input = "Raju is good boyaaAA".toLowerCase();
		String vowels = "aeiou";
		long vowelSize = Arrays.asList(input.split("")).stream().filter(inp -> vowels.contains(inp)).count();
		System.out.println("Vowel size is  -> " + vowelSize);
	}

}
