1.Lamda Expressions
	Lambda expression helps us to write our code in functional style.
	It is very useful in collection library in which it helps to iterate, filter and extract data.
	Lambda expression provides implementation of functional interface.
	
	interface Addable{  
		int add(int a,int b);  
	}  
  
	public class LambdaExpressionEx{  
		public static void main(String[] args) {  
          
			Addable ad1=(a,b)->(a+b);  
			System.out.println(ad1.add(10,20));  
                  
		}  
	}  	
	
	
	
	
	
2.forEach 
	
	list.forEach( n->System.out.println(n) );  
	
	
        List<String> gamesList = new ArrayList<String>();  
        gamesList.add("Football");  
        gamesList.add("Cricket");  

        gamesList.forEach(games -> System.out.println(games)); 

		
	
2. Stream
	List<Product> productsList = new ArrayList<Product>();  
        productsList.add(new Product(1,"HP Laptop",25000f));  
        productsList.add(new Product(2,"Dell Laptop",30000f));  

        List<Float> productPriceList2 =productsList.stream()  
                                     .filter(p -> p.price > 30000)// filtering data  
                                     .map(p->p.price)        // fetching price  
                                     .collect(Collectors.toList());


	filter(),map(),flatMap(),distinct(),limit(),sorted(),count(),
	max(),min(),reduce(),allMatch(),anyMatch(),noneMatch()